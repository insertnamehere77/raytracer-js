{
  "name": "rt",
  "version": "1.0.0",
  "description": "Port of an old raytracer I wrote in college from C++ to JS",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack --config webpack.config.js",
    "start": "npm run build && node server.js",
    "start-dev": "NODE_ENV=development npm run start"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "webpack": "^5.19.0"
  },
  "devDependencies": {
    "webpack-cli": "^4.4.0"
  }
}